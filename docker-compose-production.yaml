services:
  haproxy-airflow-three:
    image: haproxy:2.6
    ports:
      - 8443:8443
    volumes:
      - ${AIRFLOW_PROJ_DIR}/haproxy/ssl:/usr/local/etc/haproxy/ssl
      - ${AIRFLOW_PROJ_DIR}/haproxy/haproxy-production.cfg:/usr/local/etc/haproxy/haproxy.cfg
    depends_on:
      - airflow-apiserver
